# ⚠️ DATABASE CONFIGURATION ⚠️
# - If running LOCALLY (without Docker), use:
#   DB_HOST=localhost
# - If running INSIDE DOCKER, use:
#   DB_HOST=db (the MySQL service name in docker-compose)

# 🔗 Configuração MySQL
DB_HOST=                        # Nome do serviço MySQL no docker-compose
DB_PORT=                        # 🚪 Porta do banco de dados (por padrão, 3306).
DB_USERNAME=                    # 👤 Usuário do banco de dados.
DB_PASSWORD=                    # 🔑 Senha do banco de dados.
DB_DATABASE=                    # 🗄️ Nome do banco de dados.
